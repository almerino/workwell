{
  "name": "nevereatalone",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "6.9.x"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd client/ && yarn --dev && yarn && yarn build"
  },
  "cacheDirectories": ["node_modules", "client/node_modules"],
  "dependencies": {
    "express": "^4.15.2",
    "graphql-anywhere": "^3.0.1",
    "morgan": "^1.8.1"
  }
}
